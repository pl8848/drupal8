{#
/**
 * @file
 * Default theme implementation to display a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region <div>.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 *
 * @ingroup themeable
 */
#}
{% if content %}
  <div{{ attributes }}>
	<div id="fh5co-header" class="fh5co-cover" role="banner" style="background-image:url({{base_path}}themes/test/images/img_bg_2.jpg);" data-stellar-background-ratio="0.5"> 
    {{ content }}
  </div>
  </div>
{% endif %}
